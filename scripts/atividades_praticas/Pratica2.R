### Referentes aos videos 4 e 5


# Importe o conjunto de dados `alunos.csv` e tente fazer as seguintes tarefas:
  
# 1. Inspecione as colunas para saber como as variaveis estao codificadas;

# 2. Transforme a variavel `genero` e a variavel `estimacao` em um fator e veja quantos niveis essas variaveis tem;

# 3. Veja quantos valores unicos a variavel `irmaos` tem;

# 4. Crie um subconjunto chamado `alunos.gato` que contenha observacoes das variaveis `ano`, `idade` e `genero` apenas dos alunos que tenham apenas gato como animal de estimacao. Nao se esqueca do `drop_levels()` ao fim do codigo!
  
# 5. Use a funcao `str()` para conferir se voce conseguiu criar o subconjunto `alunos.gatos` satisfatoriamente. 

#Os dados presentes no arquivo `alunos.csv` foram coletados ao longo de quatro anos. Esse arquivo contem as respostas de alunos ingressantes no curso de Bacharelado em Estatistica da UFRN a um questionario aplicado durante o primeiro mes de aulas em cada ano. Cada linha representa um aluno e suas colunas sao:
  
# `ano`: ano de ingresso no curso
# `genero`: masculino ou feminino
# `idade`: em anos
# `altura`: em cm
# `peso`: em kg
# `calcado`: numero do calcado
# `irmaos`: numero de irmaos
# `namorado`: se possui namorado(a) ou conjuge
# `fumante`: fuma ou nao
# `alcool`: numero de vezes que consome alcool semanalmente
# `estimacao`: tipo de animal de estimacao
# `exercicio`: numero de horas semanais de exercicio fisico
# `enem`: nota obtida no ENEM
# `sono`: horas diarias de sono
# `ufrn`: opiniao sobre a estrutura da UFRN


